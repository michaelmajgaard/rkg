# rkg

**Disclaimer:** Project build as a learning excercise *[C]*. **DO NOT USE** for anything real world.

Random key/password generator written in C.

```
usage:
  rkg -k <key-length> [-n]

options:
  -k --key-length {>8} Key length in characters.
  -n                   Omit trailing newline in output.
```
